enum SocialEnum { google, kakao, naver, apple, facebook }

enum ProductCategory {
  all,
  alcohol,
  beverage,
  candy,
  frozen,
  ice,
  instant,
  lifeuse,
  medic,
  mlik,
  noodle,
  onedate,
  snack,
  tobacco
}

enum Route { login, register }

enum WidgetColor { grey, skyblue }

enum WidgetSize { small, big }
